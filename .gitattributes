# 统一行尾为 LF —— 本仓库的脚本要在 Windows / Linux 两边跑，
# 尤其 deploy-vps.sh 若被转成 CRLF，在 Linux 上会直接报
# "/bin/bash^M: bad interpreter" 无法执行。
* text=auto eol=lf

# 明确二进制类型，避免被当文本处理
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.zip binary
*.tgz binary
